import { useState } from 'react'; export default function Upload(){ const [uploaded, setUploaded] = useState(false); const [fileName, setFileName] = useState(''); const handleFile = (e) => { const file = e.target.files?.[0]; if(file){ setFileName(file.name); setUploaded(true); } }; return (<div style={{color:'white'}}><h2 style={{fontSize:'24px',fontWeight:'bold',marginBottom:'8px'}}>Upload Players</h2><p style={{color:'#9ca3af',marginBottom:'32px'}}>Import your player dataset via CSV</p><div style={{border:'2px dashed #1f2937',borderRadius:'12px',padding:'80px',textAlign:'center',background:'#111827',marginBottom:'24px'}}>{uploaded ? (<div><p style={{fontSize:'48px'}}>✅</p><p style={{fontWeight:'bold',marginTop:'8px'}}>{fileName} uploaded!</p><p style={{color:'#9ca3af',fontSize:'14px',marginTop:'4px'}}>Ready to import</p></div>) : (<div><p style={{fontSize:'48px'}}>📂</p><p style={{fontWeight:'500',marginTop:'8px'}}>Drag and drop your CSV file here</p><p style={{color:'#9ca3af',margin:'8px 0'}}>or</p><label style={{background:'#3b82f6',color:'white',padding:'8px 24px',borderRadius:'8px',cursor:'pointer',fontSize:'14px'}}>Browse File<input type='file' accept='.csv' style={{display:'none'}} onChange={handleFile} /></label></div>)}</div>{uploaded && (<button style={{width:'100%',background:'#3b82f6',color:'white',border:'none',borderRadius:'12px',padding:'16px',fontSize:'16px',fontWeight:'600',cursor:'pointer'}}>Import Players</button>)}</div>)}
